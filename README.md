# Vashu's Dotfiles (DE Setup)
This repository contains my personal Linux configuration for Data Engineering.

## Features:
- **Smart Venv**: Automatically activates .venv in parent/sub-directories.
- **Custom Aliases**: `gp` for Git, `cls` for clear, `checkup` for system stats.
- **Clean UI**: Minimalistic PS1 prompt.